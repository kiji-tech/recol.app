import { ReactNode } from 'react';
import { View, Text } from 'react-native';

export default function DefaultScheduleGroup(): ReactNode {
  return (
    <View>
      <Text>Calendar</Text>
    </View>
  );
}
